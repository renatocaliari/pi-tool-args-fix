{
  "name": "pi-tool-repair-layer",
  "version": "0.7.0-alpha",
  "description": "Intercepts tool calls and repairs common LLM argument mistakes before execution — null stripping, JSON parsing, array wrapping, markdown path cleanup, relational defaults, and schema-aware extra property stripping.",
  "type": "module",
  "main": "./index.ts",
  "exports": {
    ".": "./index.ts",
    "./repairs": "./repairs.ts",
    "./recorder": "./recorder.ts",
    "./stats": "./stats.ts"
  },
  "keywords": [
    "pi-package",
    "pi-extension",
    "tool-calling",
    "repair",
    "llm"
  ],
  "author": "Cali",
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^3.1.1",
    "@vitest/coverage-v8": "^3.1.1"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  }
}
